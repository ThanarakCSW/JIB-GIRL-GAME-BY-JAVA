package com.jibgirl.Relationship;

// คลาสนี้ทำหน้าที่จัดการ "ค่าความสัมพันธ์"
// ไม่มีโค้ด UI เกี่ยวข้อง เป็น Logic ล้วน ๆ
public class Relationship {

    // เก็บค่าความสัมพันธ์ปัจจุบัน (0-100)
    private int relationship;

    // Constructor ใช้กำหนดค่าเริ่มต้น
    public Relationship(int startValue) {
        relationship = startValue;
    }

    // เมธอดสำหรับเพิ่ม / ลด ความสัมพันธ์
    public void update(int value) {
        relationship += value;

        // ป้องกันค่าเกินขอบเขต
        if (relationship > 100) relationship = 100;
        if (relationship < 0) relationship = 0;
    }

    // คืนค่าความสัมพันธ์ปัจจุบัน
    public int getValue() {
        return relationship;
    }

    // ตรวจสอบเงื่อนไขตอนจบตามคะแนน
    public String getEnding() {

        // คะแนนสูงมาก
        if (relationship >= 80) {
            return "❤️ เธอตกลงคบกับคุณ! (Good Ending)";
        }
        // คะแนนระดับกลาง
        else if (relationship >= 40) {
            return "🙂 เธอขอเป็นเพื่อนกับคุณ (Normal Ending)";
        }
        // คะแนนต่ำ
        else {
            return "💔 เธอปฏิเสธคุณ (Bad Ending)";
        }
    }
}
